"use client";

import { useSession } from "next-auth/react";

export function PlanCard() {
  // Component removed - badges no longer needed
  // Plan badge is already shown in UsageProgress component
  return null;
}

